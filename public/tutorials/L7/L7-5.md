# L7-5: Skill 库管理

> 建立可复用、可维护的 Skill 资产库

## 本节导读

随着 Skill 数量的增加，有效的管理变得至关重要。本节课将教你如何建立和管理 Skill 库，实现 Skill 的复用和共享。

通过本节课，你将学会：
- Skill 库的组织结构
- Skill 版本控制策略
- Skill 共享和分发
- 实战：建立团队 Skill 库

---

## 一、Skill 库架构

### 1.1 目录结构

```
skill-library/
├── README.md                    # 库说明
├── index.json                   # Skill 索引
│
├── categories/                  # 按类别组织
│   ├── code-review/             # 代码审查
│   │   ├── react-reviewer/
│   │   ├── security-auditor/
│   │   └── performance-checker/
│   │
│   ├── code-generation/         # 代码生成
│   │   ├── component-generator/
│   │   ├── test-generator/
│   │   └── api-generator/
│   │
│   ├── documentation/           # 文档生成
│   │   ├── readme-writer/
│   │   └── api-doc-generator/
│   │
│   └── analysis/                # 分析类
│       ├── dependency-analyzer/
│       └── complexity-analyzer/
│
├── shared/                      # 共享资源
│   ├── templates/               # 模板
│   ├── knowledge/               # 知识库
│   └── utils/                   # 工具函数
│
└── registry/                    # 注册表
    ├── local/                   # 本地 Skill
    └── remote/                  # 远程 Skill
```

### 1.2 Skill 元数据

```json
{
  "id": "react-reviewer",
  "name": "React 代码审查专家",
  "version": "2.1.0",
  "description": "专门审查 React 代码的 Skill，涵盖最佳实践、性能优化、Hooks 使用等",
  "author": "AI Commander Team",
  "license": "MIT",
  "category": "code-review",
  "tags": ["react", "frontend", "review"],
  "language": ["typescript", "javascript"],
  "dependencies": [],
  "stats": {
    "downloads": 1500,
    "rating": 4.5,
    "reviews": 120
  },
  "compatibility": {
    "minVersion": "1.0.0",
    "maxVersion": "3.0.0"
  }
}
```

---

## 二、版本控制

### 2.1 版本策略

```
语义化版本：MAJOR.MINOR.PATCH

MAJOR：不兼容的 API 修改
MINOR：向下兼容的功能新增
PATCH：向下兼容的问题修复

示例：
v1.0.0 - 初始版本
v1.1.0 - 新增 Vue 支持
v1.1.1 - 修复 React 18 检测问题
v2.0.0 - 重构 API，不兼容 v1.x
```

### 2.2 版本管理

```typescript
interface SkillVersion {
  version: string;
  changelog: string;
  breaking: boolean;
  deprecated?: boolean;
  migration?: string;
}

class SkillVersionManager {
  private versions: Map<string, SkillVersion[]> = new Map();
  
  // 发布新版本
  publishVersion(skillId: string, version: SkillVersion): void {
    const skillVersions = this.versions.get(skillId) || [];
    
    // 检查版本号是否合法
    if (!this.isValidVersion(version.version)) {
      throw new Error(`Invalid version: ${version.version}`);
    }
    
    // 检查是否已存在
    if (skillVersions.some(v => v.version === version.version)) {
      throw new Error(`Version ${version.version} already exists`);
    }
    
    skillVersions.push(version);
    this.versions.set(skillId, skillVersions);
  }
  
  // 获取最新版本
  getLatestVersion(skillId: string): string | null {
    const versions = this.versions.get(skillId);
    if (!versions || versions.length === 0) return null;
    
    return versions
      .filter(v => !v.deprecated)
      .sort((a, b) => this.compareVersion(b.version, a.version))[0]?.version || null;
  }
  
  // 版本对比
  private compareVersion(v1: string, v2: string): number {
    const parts1 = v1.split('.').map(Number);
    const parts2 = v2.split('.').map(Number);
    
    for (let i = 0; i < Math.max(parts1.length, parts2.length); i++) {
      const p1 = parts1[i] || 0;
      const p2 = parts2[i] || 0;
      
      if (p1 > p2) return 1;
      if (p1 < p2) return -1;
    }
    
    return 0;
  }
}
```

---

## 三、Skill 共享

### 3.1 共享方式

```
共享方式：

1. 本地共享
   └── 团队内网共享目录
   
2. Git 仓库
   └── GitHub/GitLab 私有仓库
   
3. NPM 包
   └── 发布到私有 NPM Registry
   
4. 专用 Registry
   └── 自建 Skill Registry 服务
```

### 3.2 发布流程

```typescript
class SkillPublisher {
  async publish(skillPath: string, options: PublishOptions): Promise<void> {
    // 1. 验证 Skill
    const validation = await this.validateSkill(skillPath);
    if (!validation.valid) {
      throw new Error(`Validation failed: ${validation.errors.join(', ')}`);
    }
    
    // 2. 打包
    const packagePath = await this.packageSkill(skillPath);
    
    // 3. 上传
    await this.upload(packagePath, options.registry);
    
    // 4. 更新索引
    await this.updateIndex(options.registry);
    
    console.log(`Skill published successfully: ${validation.metadata.id}@${validation.metadata.version}`);
  }
  
  private async validateSkill(skillPath: string): Promise<ValidationResult> {
    // 检查必需文件
    const requiredFiles = ['skill.yaml', 'README.md', 'index.ts'];
    
    // 验证元数据
    // 验证代码
    // 运行测试
    
    return { valid: true, metadata: {} };
  }
}
```

---

## 四、实战：建立团队 Skill 库

### 4.1 初始化仓库

```bash
# 创建 Skill 库
mkdir team-skill-library
cd team-skill-library
git init

# 创建目录结构
mkdir -p categories/{code-review,code-generation,documentation}
mkdir -p shared/{templates,knowledge,utils}
mkdir -p registry

# 创建配置文件
cat > package.json << 'EOF'
{
  "name": "@company/skill-library",
  "version": "1.0.0",
  "description": "团队 Skill 库",
  "scripts": {
    "validate": "skill-cli validate",
    "publish": "skill-cli publish",
    "test": "skill-cli test"
  }
}
EOF
```

### 4.2 添加 Skill

```bash
# 添加 React 审查 Skill
cd categories/code-review
mkdir react-reviewer
cd react-reviewer

# 创建 Skill 文件
cat > skill.yaml << 'EOF'
skill:
  id: react-reviewer
  name: React 代码审查专家
  version: 1.0.0
  category: code-review
  language: [typescript, javascript]
  
  description: |
    专业的 React 代码审查 Skill，涵盖：
    - React 最佳实践
    - Hooks 使用规范
    - 性能优化建议
    - TypeScript 类型检查
  
  author: Frontend Team
  tags: [react, review, frontend]
EOF

# 创建提示词文件
cat > prompt.md << 'EOF'
# React 代码审查专家

## 角色
你是 React 开发专家，有 5 年以上经验...

## 审查维度
...
EOF
```

### 4.3 使用 Skill

```typescript
import { SkillLibrary } from '@company/skill-library';

// 初始化库
const library = new SkillLibrary({
  registry: 'https://skills.company.com',
  cacheDir: './.skill-cache'
});

// 搜索 Skill
const skills = await library.search({
  category: 'code-review',
  language: 'typescript'
});

// 安装 Skill
await library.install('react-reviewer', 'latest');

// 使用 Skill
const skill = await library.load('react-reviewer');
const result = await skill.execute({
  code: sourceCode,
  options: { strict: true }
});
```

---

## 五、本节小结

### 核心要点

1. **组织结构**：按类别组织，清晰的目录结构

2. **版本控制**：语义化版本，兼容管理

3. **共享分发**：多种方式，灵活选择

4. **团队实践**：建立规范，持续维护

### 下一步

**第七章完成！** 🎉

你已经掌握了 AI 代码审查的完整知识：
- L7-1: AI 代码审查概述
- L7-2: 设计专业审查 Skill
- L7-3: 审查规则定制
- L7-4: 审查结果处理
- L7-5: Skill 库管理

下一步，我们将进入 **第八章：性能优化**，学习如何让 AI 协助进行性能分析和优化。

→ [8.1 性能分析基础](/tutorial/L8-1)
