# L6-4: 代码整合与集成测试

> 将多个 Agent 的成果汇聚成完整的系统

## 本节导读

当多个 Agent 并行开发不同模块时，如何将它们的工作成果整合成一个完整的、可运行的系统？这是 Multi-Agent 协作的最后一公里。本节课将讲解代码整合的策略和集成测试的方法。

通过本节课，你将学会：
- 代码整合的流程和策略
- 接口契约管理
- 集成测试的设计和执行
- 冲突解决和版本合并

---

## 一、代码整合的挑战

### 1.1 常见问题

```
┌─────────────────────────────────────────────────────────────┐
│                    代码整合的挑战                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Agent A 开发的用户模块              Agent B 开发的订单模块   │
│  ┌─────────────────────┐            ┌─────────────────────┐ │
│  │ getUser(id)         │            │ createOrder(userId) │ │
│  │   返回: {           │            │   调用: getUser()   │ │
│  │     id, name,       │            │   期望: {           │ │
│  │     email           │            │     id, name,       │ │
│  │   }                 │            │     email, phone    │ │
│  └─────────────────────┘            │   }                 │ │
│                                     └─────────────────────┘ │
│                                                             │
│  问题：接口不匹配！                                          │
│  - Agent B 期望的 user 对象包含 phone 字段                    │
│  - 但 Agent A 返回的对象没有 phone 字段                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**常见整合问题：**

| 问题类型 | 具体表现 | 影响 |
|----------|----------|------|
| **接口不匹配** | 参数、返回值不一致 | 调用失败 |
| **数据格式冲突** | JSON 结构不同 | 数据解析错误 |
| **命名冲突** | 函数、变量重名 | 编译错误 |
| **依赖版本冲突** | 依赖库版本不同 | 运行时错误 |
| **配置冲突** | 环境变量、配置项冲突 | 部署失败 |

### 1.2 整合流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 收集代码  │───→│ 接口检查  │───→│ 合并代码  │───→│ 构建验证  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
     │               │               │               │
     ▼               ▼               ▼               ▼
  获取各 Agent     检查接口契约     解决冲突        编译运行
  的代码输出       验证兼容性       合并文件        初步验证
```

---

## 二、接口契约管理

### 2.1 契约优先开发

```
传统方式：                        契约优先：
┌──────────┐                     ┌──────────┐
│ 开发实现  │                     │ 定义契约  │
└────┬─────┘                     └────┬─────┘
     │                               │
     ▼                               ▼
┌──────────┐                     ┌──────────┐
│ 发现接口  │                     │ 并行开发  │
│ 不匹配   │                     │         │
└────┬─────┘                     └────┬─────┘
     │                               │
     ▼                               ▼
┌──────────┐                     ┌──────────┐
│ 修改代码  │                     │ 整合测试  │
│ (返工)   │                     │         │
└──────────┘                     └──────────┘
```

### 2.2 接口定义示例

```yaml
# api-contract.yaml
api:
  version: 1.0.0
  
  endpoints:
    - name: getUser
      path: /api/users/{id}
      method: GET
      request:
        parameters:
          - name: id
            type: string
            required: true
      response:
        status: 200
        body:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            email:
              type: string
            phone:
              type: string
              nullable: true
          required: [id, name, email]
    
    - name: createOrder
      path: /api/orders
      method: POST
      request:
        body:
          type: object
          properties:
            userId:
              type: string
            items:
              type: array
              items:
                type: object
                properties:
                  productId:
                    type: string
                  quantity:
                    type: number
          required: [userId, items]
      response:
        status: 201
        body:
          type: object
          properties:
            orderId:
              type: string
            status:
              type: string
              enum: [pending, confirmed]
```

### 2.3 契约验证

```typescript
class ContractValidator {
  private contract: APIContract;
  
  constructor(contractPath: string) {
    this.contract = this.loadContract(contractPath);
  }
  
  // 验证实现是否符合契约
  validateImplementation(
    endpointName: string, 
    implementation: any
  ): ValidationResult {
    const endpoint = this.contract.endpoints.find(
      e => e.name === endpointName
    );
    
    if (!endpoint) {
      return {
        valid: false,
        errors: [`未找到接口定义: ${endpointName}`]
      };
    }
    
    const errors: string[] = [];
    
    // 验证请求参数
    if (endpoint.request.parameters) {
      for (const param of endpoint.request.parameters) {
        if (param.required && !implementation.params?.[param.name]) {
          errors.push(`缺少必需参数: ${param.name}`);
        }
      }
    }
    
    // 验证响应结构
    if (endpoint.response.body) {
      const responseValidation = this.validateSchema(
        endpoint.response.body,
        implementation.response
      );
      errors.push(...responseValidation.errors);
    }
    
    return {
      valid: errors.length === 0,
      errors
    };
  }
  
  private validateSchema(schema: any, data: any): ValidationResult {
    const errors: string[] = [];
    
    if (schema.type === 'object') {
      // 验证必需字段
      for (const field of schema.required || []) {
        if (!(field in data)) {
          errors.push(`缺少必需字段: ${field}`);
        }
      }
      
      // 验证字段类型
      for (const [key, propSchema] of Object.entries(schema.properties)) {
        if (key in data) {
          const value = data[key];
          const prop = propSchema as any;
          
          if (prop.type === 'string' && typeof value !== 'string') {
            errors.push(`字段 ${key} 应为字符串类型`);
          }
          if (prop.type === 'number' && typeof value !== 'number') {
            errors.push(`字段 ${key} 应为数字类型`);
          }
        }
      }
    }
    
    return { valid: errors.length === 0, errors };
  }
}
```

---

## 三、代码合并策略

### 3.1 合并流程

```
┌─────────────────────────────────────────────────────────────┐
│                    代码合并流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Agent A 代码                    Agent B 代码               │
│  ┌─────────────┐                 ┌─────────────┐            │
│  │ src/user/   │                 │ src/order/  │            │
│  │   service.ts│                 │   service.ts│            │
│  └──────┬──────┘                 └──────┬──────┘            │
│         │                               │                   │
│         └───────────┬───────────────────┘                   │
│                     │                                       │
│                     ▼                                       │
│              ┌─────────────┐                                │
│              │  冲突检测   │                                │
│              └──────┬──────┘                                │
│                     │                                       │
│         ┌───────────┴───────────┐                          │
│         │                       │                          │
│         ▼                       ▼                          │
│    ┌─────────┐            ┌─────────┐                      │
│    │ 有冲突  │            │ 无冲突  │                      │
│    └────┬────┘            └────┬────┘                      │
│         │                       │                          │
│         ▼                       ▼                          │
│    ┌─────────┐            ┌─────────┐                      │
│    │ 解决冲突 │            │ 直接合并 │                      │
│    └────┬────┘            └────┬────┘                      │
│         │                       │                          │
│         └───────────┬───────────┘                          │
│                     │                                       │
│                     ▼                                       │
│              ┌─────────────┐                                │
│              │  合并后代码  │                                │
│              └─────────────┘                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 冲突解决

```typescript
class CodeMerger {
  // 合并代码文件
  async mergeFiles(
    baseFile: string,
    fileA: string,
    fileB: string
  ): Promise<MergeResult> {
    // 使用 Git 的合并算法
    const result = await this.gitMerge(baseFile, fileA, fileB);
    
    if (result.hasConflicts) {
      // 使用 AI 解决冲突
      const resolved = await this.resolveConflictsWithAI(
        result.conflicts
      );
      return resolved;
    }
    
    return result;
  }
  
  // AI 辅助冲突解决
  private async resolveConflictsWithAI(
    conflicts: Conflict[]
  ): Promise<MergeResult> {
    const resolvedCode: string[] = [];
    
    for (const conflict of conflicts) {
      const prompt = `
请解决以下代码合并冲突：

文件: ${conflict.filePath}

冲突区域:
<<<<<<< HEAD
${conflict.ours}
=======
${conflict.theirs}
>>>>>>> branch

上下文:
${conflict.context}

请提供合并后的代码，保留两者的合理部分。
      `;
      
      // 调用 AI 解决冲突
      const resolution = await this.aiResolver.resolve(prompt);
      resolvedCode.push(resolution);
    }
    
    return {
      mergedCode: resolvedCode.join('\n'),
      hasConflicts: false
    };
  }
}
```

---

## 四、集成测试

### 4.1 测试策略

```
测试金字塔（集成测试层）

                    ┌─────────┐
                    │  E2E   │  ← 端到端测试
                    │  10%   │     (用户场景)
                   ┌┴─────────┴┐
                   │ Integration│ ← 集成测试 ★
                   │    30%    │    (模块间交互)
                  ┌┴───────────┴┐
                  │   Unit      │ ← 单元测试
                  │    60%     │    (单个模块)
                  └─────────────┘
```

### 4.2 集成测试类型

| 测试类型 | 测试内容 | 示例 |
|----------|----------|------|
| **接口集成** | API 调用和数据流 | 前端调用后端 API |
| **数据集成** | 数据格式和一致性 | 数据库读写验证 |
| **流程集成** | 业务流程完整性 | 下单到支付流程 |
| **异常集成** | 错误处理和恢复 | 网络超时处理 |

### 4.3 集成测试实现

```typescript
// 集成测试示例
describe('订单模块集成测试', () => {
  let userService: UserService;
  let orderService: OrderService;
  let paymentService: PaymentService;
  
  beforeAll(async () => {
    // 初始化所有服务
    userService = new UserService();
    orderService = new OrderService();
    paymentService = new PaymentService();
    
    // 建立服务间连接
    orderService.setUserService(userService);
    orderService.setPaymentService(paymentService);
  });
  
  test('完整下单流程', async () => {
    // 1. 创建用户
    const user = await userService.createUser({
      name: '测试用户',
      email: 'test@example.com'
    });
    expect(user.id).toBeDefined();
    
    // 2. 创建订单
    const order = await orderService.createOrder({
      userId: user.id,
      items: [
        { productId: 'prod-1', quantity: 2 },
        { productId: 'prod-2', quantity: 1 }
      ]
    });
    expect(order.orderId).toBeDefined();
    expect(order.status).toBe('pending');
    
    // 3. 支付订单
    const payment = await paymentService.processPayment({
      orderId: order.orderId,
      amount: order.totalAmount,
      method: 'credit_card'
    });
    expect(payment.status).toBe('success');
    
    // 4. 验证订单状态更新
    const updatedOrder = await orderService.getOrder(order.orderId);
    expect(updatedOrder.status).toBe('confirmed');
  });
  
  test('库存不足场景', async () => {
    const user = await userService.createUser({
      name: '测试用户',
      email: 'test2@example.com'
    });
    
    // 尝试购买超过库存的商品
    await expect(
      orderService.createOrder({
        userId: user.id,
        items: [
          { productId: 'prod-1', quantity: 9999 }
        ]
      })
    ).rejects.toThrow('库存不足');
  });
});
```

---

## 五、本节小结

### 核心概念

1. **代码整合** 需要处理接口匹配、数据格式、命名冲突等问题

2. **契约优先** 开发可以避免后期的接口不匹配问题

3. **集成测试** 验证模块间的协作是否正常

4. **冲突解决** 可以借助 AI 辅助，提高效率

### 整合检查清单

- [ ] 接口契约已定义并验证
- [ ] 代码合并冲突已解决
- [ ] 集成测试用例已编写
- [ ] 端到端流程已验证
- [ ] 异常场景已测试

### 下一步

在下一节课 [L6-5: 版本控制与 AI](/tutorial/L6-5) 中，我们将：
- 学习 AI 如何辅助版本控制
- 掌握智能提交和分支管理
- 了解代码审查自动化
- 实战：AI 辅助的 Git 工作流

---

→ [6.5 版本控制与 AI](/tutorial/L6-5)
