# L5-4: 工具集成与扩展

> 让 Agent 连接世界，扩展无限可能

## 本节导读

技能的核心价值在于能够**调用外部工具**完成实际任务。本节课将深入讲解如何将各种工具集成到 Agent 技能中，让 AI 真正具备"动手能力"。

通过本节课，你将学会：
- 工具集成的架构设计
- MCP 协议的使用方法
- Function Calling 的实现
- 实战：开发一个文件操作工具

---

## 一、为什么需要工具集成

### 1.1 AI 的"手"和"脚"

**类比理解：**

```
┌─────────────────────────────────────────────────────────────┐
│                    AI 的能力边界                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   只有大脑（模型）              大脑 + 工具（Agent）          │
│   ┌─────────────┐               ┌─────────────┐            │
│   │   思考能力   │               │   思考能力   │            │
│   │   ▓▓▓▓▓▓▓   │               │   ▓▓▓▓▓▓▓   │            │
│   └─────────────┘               └─────────────┘            │
│                                 ┌─────────────┐            │
│                                 │   执行能力   │            │
│                                 │   ▓▓▓▓▓▓▓   │            │
│                                 └─────────────┘            │
│                                                             │
│   能思考，但不能行动            能思考，也能行动             │
│   - 无法获取实时信息            - 读取文件、查询数据库        │
│   - 无法操作外部环境            - 调用 API、执行命令         │
│   - 无法验证执行结果            - 运行测试、验证代码         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 工具集成的价值

| 能力 | 无工具 | 有工具 |
|------|--------|--------|
| **信息获取** | 依赖训练数据，可能过时 | 实时查询数据库、API |
| **代码执行** | 只能生成代码 | 能运行测试、验证结果 |
| **环境操作** | 无法影响外部环境 | 读写文件、执行命令 |
| **协作能力** | 单轮对话 | 多轮交互、工具链协作 |

---

## 二、工具集成架构

### 2.1 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                    工具集成架构                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    Agent 核心                         │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │  │  意图理解   │  │  任务规划   │  │  结果整合   │  │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  │  │
│  └──────────────────────┬───────────────────────────────┘  │
│                         │                                   │
│  ┌──────────────────────┴───────────────────────────────┐  │
│  │                   工具管理层                          │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │  │  工具发现   │  │  参数映射   │  │  结果解析   │  │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  │  │
│  └──────────────────────┬───────────────────────────────┘  │
│                         │                                   │
│  ┌──────────────────────┴───────────────────────────────┐  │
│  │                   工具层                              │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │  │
│  │  │文件系统 │ │ 数据库  │ │  API   │ │ 命令行  │   │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘   │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │  │
│  │  │代码执行 │ │ 浏览器  │ │ Git   │ │ Docker │   │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 工具类型分类

| 类型 | 功能 | 示例 |
|------|------|------|
| **文件系统** | 读写文件、目录操作 | 读取代码、写入配置 |
| **数据库** | 数据查询、更新 | SQL 查询、NoSQL 操作 |
| **API** | 外部服务调用 | REST API、GraphQL |
| **代码执行** | 运行代码、测试 | 执行脚本、运行测试 |
| **命令行** | 系统命令执行 | Git、Docker、Shell |
| **浏览器** | Web 操作、抓取 | 页面截图、自动化测试 |

---

## 三、MCP 协议详解

### 3.1 什么是 MCP

**MCP (Model Context Protocol)** 是 Anthropic 推出的开放标准协议，让 AI 能够安全地连接外部工具和数据源。

**核心设计理念：**
- **标准化**：统一接口，工具可互换
- **安全性**：用户控制权限
- **可扩展**：易于添加新工具

### 3.2 MCP 架构

```
┌─────────────────────────────────────────────────────────────┐
│                      MCP 架构                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────────┐         ┌─────────────┐                  │
│   │   AI 模型    │←───────→│  MCP 主机    │                  │
│   │  (Claude)   │         │  (Host)     │                  │
│   └─────────────┘         └──────┬──────┘                  │
│                                  │                          │
│                    ┌─────────────┼─────────────┐            │
│                    │             │             │            │
│              ┌─────┴────┐  ┌─────┴────┐  ┌─────┴────┐      │
│              │ MCP 服务 │  │ MCP 服务 │  │ MCP 服务 │      │
│              │ (文件)   │  │ (数据库) │  │ (API)   │      │
│              └─────────┘  └─────────┘  └─────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 MCP 服务示例

**文件系统服务：**

```typescript
// mcp-filesystem-server.ts
import { Server } from '@modelcontextprotocol/sdk/server/index.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';

const server = new Server({
  name: 'filesystem-server',
  version: '1.0.0'
}, {
  capabilities: {
    tools: {}
  }
});

// 定义可用工具
server.setRequestHandler('tools/list', async () => {
  return {
    tools: [
      {
        name: 'read_file',
        description: '读取文件内容',
        inputSchema: {
          type: 'object',
          properties: {
            path: { type: 'string', description: '文件路径' }
          },
          required: ['path']
        }
      },
      {
        name: 'write_file',
        description: '写入文件内容',
        inputSchema: {
          type: 'object',
          properties: {
            path: { type: 'string', description: '文件路径' },
            content: { type: 'string', description: '文件内容' }
          },
          required: ['path', 'content']
        }
      },
      {
        name: 'list_directory',
        description: '列出目录内容',
        inputSchema: {
          type: 'object',
          properties: {
            path: { type: 'string', description: '目录路径' }
          },
          required: ['path']
        }
      }
    ]
  };
});

// 处理工具调用
server.setRequestHandler('tools/call', async (request) => {
  const { name, arguments: args } = request.params;
  
  switch (name) {
    case 'read_file':
      const content = await fs.readFile(args.path, 'utf-8');
      return {
        content: [{ type: 'text', text: content }]
      };
    
    case 'write_file':
      await fs.writeFile(args.path, args.content);
      return {
        content: [{ type: 'text', text: '文件写入成功' }]
      };
    
    case 'list_directory':
      const entries = await fs.readdir(args.path, { withFileTypes: true });
      const result = entries.map(e => ({
        name: e.name,
        type: e.isDirectory() ? 'directory' : 'file'
      }));
      return {
        content: [{ type: 'text', text: JSON.stringify(result, null, 2) }]
      };
    
    default:
      throw new Error(`未知工具: ${name}`);
  }
});

// 启动服务器
const transport = new StdioServerTransport();
await server.connect(transport);
```

### 3.4 MCP 客户端使用

```typescript
// mcp-client.ts
import { Client } from '@modelcontextprotocol/sdk/client/index.js';
import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio.js';

// 创建客户端
const client = new Client({
  name: 'ai-commander',
  version: '1.0.0'
});

// 连接到 MCP 服务
const transport = new StdioClientTransport({
  command: 'node',
  args: ['mcp-filesystem-server.js']
});

await client.connect(transport);

// 获取可用工具列表
const tools = await client.listTools();
console.log('可用工具:', tools);

// 调用工具
const result = await client.callTool({
  name: 'read_file',
  arguments: { path: '/path/to/file.ts' }
});

console.log('文件内容:', result);
```

---

## 四、Function Calling 实现

### 4.1 Function Calling 原理

**流程：**

```
用户请求 → AI 分析 → 决定调用 → 执行函数 → 返回结果 → AI 整合 → 最终回复
              ↓           ↓           ↓
           识别意图    提取参数    执行操作
```

### 4.2 OpenAI Function Calling

```typescript
import OpenAI from 'openai';

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

// 定义可用函数
const functions = [
  {
    name: 'get_current_weather',
    description: '获取指定城市的当前天气',
    parameters: {
      type: 'object',
      properties: {
        location: {
          type: 'string',
          description: '城市名称，如：北京、上海'
        },
        unit: {
          type: 'string',
          enum: ['celsius', 'fahrenheit'],
          description: '温度单位'
        }
      },
      required: ['location']
    }
  },
  {
    name: 'search_code',
    description: '在代码库中搜索代码',
    parameters: {
      type: 'object',
      properties: {
        query: {
          type: 'string',
          description: '搜索关键词'
        },
        file_type: {
          type: 'string',
          enum: ['.ts', '.tsx', '.js', '.jsx'],
          description: '文件类型过滤'
        }
      },
      required: ['query']
    }
  },
  {
    name: 'run_tests',
    description: '运行单元测试',
    parameters: {
      type: 'object',
      properties: {
        test_pattern: {
          type: 'string',
          description: '测试文件匹配模式'
        },
        coverage: {
          type: 'boolean',
          description: '是否生成覆盖率报告'
        }
      }
    }
  }
];

// 调用 AI
async function chatWithFunctions(userMessage: string) {
  const messages = [
    { role: 'user', content: userMessage }
  ];
  
  const response = await openai.chat.completions.create({
    model: 'gpt-4',
    messages,
    functions,
    function_call: 'auto'
  });
  
  const message = response.choices[0].message;
  
  // 检查是否需要调用函数
  if (message.function_call) {
    const functionName = message.function_call.name;
    const args = JSON.parse(message.function_call.arguments);
    
    console.log(`AI 决定调用函数: ${functionName}`);
    console.log(`参数:`, args);
    
    // 执行函数
    const functionResult = await executeFunction(functionName, args);
    
    // 将结果返回给 AI
    messages.push(message);
    messages.push({
      role: 'function',
      name: functionName,
      content: JSON.stringify(functionResult)
    });
    
    // 获取最终回复
    const finalResponse = await openai.chat.completions.create({
      model: 'gpt-4',
      messages
    });
    
    return finalResponse.choices[0].message.content;
  }
  
  return message.content;
}

// 函数执行映射
async function executeFunction(name: string, args: any) {
  switch (name) {
    case 'get_current_weather':
      return await getWeather(args.location, args.unit);
    case 'search_code':
      return await searchCode(args.query, args.file_type);
    case 'run_tests':
      return await runTests(args.test_pattern, args.coverage);
    default:
      throw new Error(`未知函数: ${name}`);
  }
}
```

### 4.3 多轮 Function Calling

```typescript
// 处理复杂的多步骤任务
async function handleComplexTask(userRequest: string) {
  const messages = [
    {
      role: 'system',
      content: '你是一个开发助手，可以帮助用户分析代码、运行测试和修复问题。'
    },
    { role: 'user', content: userRequest }
  ];
  
  let maxIterations = 10;
  
  while (maxIterations > 0) {
    const response = await openai.chat.completions.create({
      model: 'gpt-4',
      messages,
      functions,
      function_call: 'auto'
    });
    
    const message = response.choices[0].message;
    
    // 如果没有函数调用，任务完成
    if (!message.function_call) {
      return message.content;
    }
    
    // 执行函数调用
    const functionName = message.function_call.name;
    const args = JSON.parse(message.function_call.arguments);
    const result = await executeFunction(functionName, args);
    
    // 添加到消息历史
    messages.push(message);
    messages.push({
      role: 'function',
      name: functionName,
      content: JSON.stringify(result)
    });
    
    maxIterations--;
  }
  
  return '达到最大迭代次数';
}

// 使用示例
const result = await handleComplexTask(`
  我发现项目中有性能问题，请帮我：
  1. 搜索所有使用 useEffect 的组件
  2. 分析是否有优化空间
  3. 运行测试确保修改后没有问题
`);
```

---

## 五、实战：开发文件操作工具

### 5.1 工具设计

**目标：** 开发一个支持代码审查的文件操作工具

**功能：**
- 读取项目文件
- 搜索代码
- 分析文件结构
- 生成文件报告

### 5.2 工具实现

```typescript
// tools/FileTool.ts
import * as fs from 'fs/promises';
import * as path from 'path';
import { glob } from 'glob';

export interface FileToolConfig {
  rootPath: string;
  allowedExtensions: string[];
  maxFileSize: number; // bytes
}

export class FileTool {
  private config: FileToolConfig;
  
  constructor(config: FileToolConfig) {
    this.config = config;
  }
  
  // 读取文件内容
  async readFile(filePath: string): Promise<string> {
    const fullPath = path.join(this.config.rootPath, filePath);
    
    // 安全检查：确保在根目录内
    if (!fullPath.startsWith(this.config.rootPath)) {
      throw new Error('访问被拒绝：路径超出项目范围');
    }
    
    // 检查文件扩展名
    const ext = path.extname(filePath);
    if (!this.config.allowedExtensions.includes(ext)) {
      throw new Error(`不支持的文件类型: ${ext}`);
    }
    
    // 检查文件大小
    const stats = await fs.stat(fullPath);
    if (stats.size > this.config.maxFileSize) {
      throw new Error(`文件过大: ${stats.size} bytes`);
    }
    
    return await fs.readFile(fullPath, 'utf-8');
  }
  
  // 搜索代码
  async searchCode(pattern: string, filePattern?: string): Promise<SearchResult[]> {
    const files = await glob(filePattern || '**/*.{ts,tsx,js,jsx}', {
      cwd: this.config.rootPath,
      ignore: ['node_modules/**', 'dist/**', '.git/**']
    });
    
    const results: SearchResult[] = [];
    
    for (const file of files) {
      const content = await this.readFile(file);
      const lines = content.split('\n');
      
      lines.forEach((line, index) => {
        if (line.includes(pattern)) {
          results.push({
            file,
            line: index + 1,
            content: line.trim()
          });
        }
      });
    }
    
    return results;
  }
  
  // 分析项目结构
  async analyzeProjectStructure(): Promise<ProjectStructure> {
    const structure: ProjectStructure = {
      directories: [],
      files: [],
      stats: {
        totalFiles: 0,
        totalLines: 0,
        byExtension: {}
      }
    };
    
    const files = await glob('**/*', {
      cwd: this.config.rootPath,
      ignore: ['node_modules/**', 'dist/**', '.git/**'],
      nodir: false
    });
    
    for (const file of files) {
      const fullPath = path.join(this.config.rootPath, file);
      const stats = await fs.stat(fullPath);
      
      if (stats.isDirectory()) {
        structure.directories.push(file);
      } else {
        const ext = path.extname(file);
        structure.stats.byExtension[ext] = (structure.stats.byExtension[ext] || 0) + 1;
        
        if (this.config.allowedExtensions.includes(ext)) {
          const content = await this.readFile(file);
          const lines = content.split('\n').length;
          
          structure.files.push({
            path: file,
            size: stats.size,
            lines
          });
          
          structure.stats.totalFiles++;
          structure.stats.totalLines += lines;
        }
      }
    }
    
    return structure;
  }
  
  // 生成文件报告
  async generateReport(outputPath: string): Promise<void> {
    const structure = await this.analyzeProjectStructure();
    
    const report = `
# 项目代码分析报告

生成时间: ${new Date().toISOString()}

## 统计概览

- 总文件数: ${structure.stats.totalFiles}
- 总行数: ${structure.stats.totalLines}
- 平均文件行数: ${Math.round(structure.stats.totalLines / structure.stats.totalFiles)}

## 文件类型分布

${Object.entries(structure.stats.byExtension)
  .map(([ext, count]) => `- ${ext || '无扩展名'}: ${count} 个文件`)
  .join('\n')}

## 最大的 10 个文件

${structure.files
  .sort((a, b) => b.lines - a.lines)
  .slice(0, 10)
  .map((f, i) => `${i + 1}. ${f.path} (${f.lines} 行)`)
  .join('\n')}
`;
    
    await fs.writeFile(outputPath, report);
  }
}

// 类型定义
interface SearchResult {
  file: string;
  line: number;
  content: string;
}

interface ProjectStructure {
  directories: string[];
  files: Array<{
    path: string;
    size: number;
    lines: number;
  }>;
  stats: {
    totalFiles: number;
    totalLines: number;
    byExtension: Record<string, number>;
  };
}
```

### 5.3 集成到技能

```typescript
// skills/CodeReviewSkill.ts
import { FileTool } from '../tools/FileTool';
import { OpenAI } from 'openai';

export class CodeReviewSkill {
  private fileTool: FileTool;
  private openai: OpenAI;
  
  constructor(config: SkillConfig) {
    this.fileTool = new FileTool(config.fileTool);
    this.openai = new OpenAI({ apiKey: config.openaiApiKey });
  }
  
  async reviewFile(filePath: string): Promise<ReviewResult> {
    // 1. 读取文件
    const code = await this.fileTool.readFile(filePath);
    
    // 2. 分析代码
    const analysis = await this.analyzeCode(code, filePath);
    
    // 3. 生成报告
    const report = await this.generateReport(analysis);
    
    return {
      file: filePath,
      issues: analysis.issues,
      suggestions: analysis.suggestions,
      report
    };
  }
  
  async reviewProject(): Promise<ProjectReviewResult> {
    // 1. 分析项目结构
    const structure = await this.fileTool.analyzeProjectStructure();
    
    // 2. 识别关键文件
    const keyFiles = this.identifyKeyFiles(structure);
    
    // 3. 审查关键文件
    const results: ReviewResult[] = [];
    for (const file of keyFiles) {
      const result = await this.reviewFile(file);
      results.push(result);
    }
    
    // 4. 生成项目级报告
    const projectReport = await this.generateProjectReport(results, structure);
    
    return {
      structure,
      fileReviews: results,
      projectReport
    };
  }
  
  private async analyzeCode(code: string, filePath: string): Promise<CodeAnalysis> {
    const response = await this.openai.chat.completions.create({
      model: 'gpt-4',
      messages: [
        {
          role: 'system',
          content: '你是代码审查专家，请分析代码并提供改进建议。'
        },
        {
          role: 'user',
          content: `
请分析以下代码文件: ${filePath}

\`\`\`
${code}
\`\`\`

请从以下维度分析：
1. 安全问题
2. 性能问题
3. 代码规范
4. 可维护性

以 JSON 格式返回结果。
          `
        }
      ],
      response_format: { type: 'json_object' }
    });
    
    return JSON.parse(response.choices[0].message.content);
  }
  
  private identifyKeyFiles(structure: ProjectStructure): string[] {
    // 识别关键文件：入口文件、核心模块、大型文件等
    return structure.files
      .filter(f => {
        // 入口文件
        if (f.path.includes('index.') || f.path.includes('main.')) return true;
        // 大型文件（超过 300 行）
        if (f.lines > 300) return true;
        // 核心模块
        if (f.path.includes('/core/') || f.path.includes('/utils/')) return true;
        return false;
      })
      .map(f => f.path)
      .slice(0, 20); // 最多审查 20 个文件
  }
}
```

---

## 六、工具安全与权限控制

### 6.1 安全原则

```
┌─────────────────────────────────────────────────────────────┐
│                    工具安全原则                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 最小权限原则                                             │
│     └── 只授予完成任务所需的最小权限                          │
│                                                             │
│  2. 路径限制                                                 │
│     └── 限制可访问的文件系统范围                              │
│                                                             │
│  3. 操作审计                                                 │
│     └── 记录所有工具调用，便于追溯                            │
│                                                             │
│  4. 用户确认                                                 │
│     └── 敏感操作需要用户确认                                  │
│                                                             │
│  5. 沙箱执行                                                 │
│     └── 在隔离环境中执行不可信代码                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 权限配置示例

```yaml
# permissions.yaml
tools:
  file_system:
    allowed_paths:
      - ./src
      - ./tests
      - ./docs
    denied_paths:
      - ./node_modules
      - ./.env
      - ./secrets
    allowed_operations:
      - read
      - write
    denied_operations:
      - delete
      - execute
  
  command_execution:
    allowed_commands:
      - npm test
      - npm run build
      - git status
    denied_commands:
      - rm -rf
      - sudo
    require_confirmation: true
  
  api_calls:
    allowed_hosts:
      - api.github.com
      - api.openai.com
    rate_limit: 100/hour
```

---

## 七、本节小结

### 核心要点

1. **工具集成是 Agent 的"手脚"**，让 AI 从思考走向行动

2. **MCP 协议** 提供了标准化的工具集成方案，安全、可扩展

3. **Function Calling** 让 AI 能够自主决定何时调用工具

4. **安全第一**，必须实施权限控制和操作审计

### 工具开发检查清单

- [ ] 明确工具的功能和边界
- [ ] 设计清晰的接口
- [ ] 实现错误处理
- [ ] 添加权限控制
- [ ] 记录操作日志
- [ ] 编写使用文档
- [ ] 测试各种场景

### 下一步

在下一节课 [L5-5: AI 辅助 Debug](/tutorial/L5-5) 中，我们将：
- 学习如何使用 AI 进行问题诊断
- 掌握调试信息的收集和分析
- 了解常见的 Debug 模式
- 实战：开发一个智能 Debug 助手

---

→ [5.5 AI 辅助 Debug](/tutorial/L5-5)
