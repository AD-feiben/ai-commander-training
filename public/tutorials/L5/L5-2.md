# L5-2: 技能开发实战

> 从零开始开发一个完整的 Agent 技能

## 本节导读

在 L5-1 中我们了解了技能系统的概念和架构，本节课将带你**动手实践**，从零开发一个完整的技能。我们将以**代码审查技能**为例，完整展示技能开发的整个流程。

通过本节课，你将学会：
- 如何设计和定义技能
- 如何构建技能知识库
- 如何集成工具
- 如何测试和优化技能

---

## 一、技能开发流程概览

### 1.1 开发流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    技能开发流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  阶段 1          阶段 2          阶段 3          阶段 4      │
│  需求分析   →   技能设计   →   开发实现   →   测试优化      │
│                                                             │
│  ├─ 确定目标    ├─ 定义结构    ├─ 编写配置    ├─ 单元测试    │
│  ├─ 分析场景    ├─ 构建知识库  ├─ 集成工具    ├─ 集成测试    │
│  ├─ 识别痛点    ├─ 设计工作流  ├─ 编写模板    ├─ 效果评估    │
│  └─ 制定标准    └─ 定义接口    └─ 实现逻辑    └─ 迭代优化    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 我们要开发的技能：智能代码审查助手

**技能名称：** CodeReview Assistant

**技能目标：**
- 自动分析代码中的潜在问题
- 提供具体的修复建议
- 支持多种编程语言
- 可集成到 CI/CD 流程

**核心功能：**
1. 安全漏洞检测
2. 性能问题识别
3. 代码规范检查
4. 最佳实践建议

---

## 二、阶段 1：需求分析

### 2.1 确定目标用户和场景

**目标用户：**
- 初级开发者：学习代码规范，提升代码质量
- 中级开发者：快速发现潜在问题
- 技术负责人：把控团队代码质量

**使用场景：**

| 场景 | 频率 | 痛点 |
|------|------|------|
| 提交代码前自查 | 每次提交 | 容易遗漏问题 |
| Code Review | 每次 PR | 审查效率低 |
| 历史代码审计 | 定期 | 代码量大，难以全面检查 |
| 新人培训 | 持续 | 需要反复讲解规范 |

### 2.2 功能需求清单

```yaml
功能需求:
  核心功能:
    - 支持 JavaScript/TypeScript 代码审查
    - 支持 Python 代码审查
    - 检测安全漏洞（SQL 注入、XSS 等）
    - 检测性能问题（N+1 查询、内存泄漏等）
    - 检查代码规范（命名、注释、复杂度）
    
  扩展功能:
    - 生成审查报告
    - 提供修复代码示例
    - 支持自定义规则
    - 集成 Git Hook
    - CI/CD 集成
    
  非功能需求:
    - 审查时间 < 30 秒（1000 行代码）
    - 误报率 < 10%
    - 漏报率 < 5%
```

### 2.3 竞品分析

| 工具 | 优点 | 缺点 | 我们的差异化 |
|------|------|------|-------------|
| ESLint | 规则丰富 | 仅语法层面 | 结合 AI 理解业务逻辑 |
| SonarQube | 全面 | 配置复杂 | 轻量、易集成 |
| CodeClimate | 可视化好 | 付费 | 免费、可定制 |
| GitHub Copilot | AI 驱动 | 审查能力弱 | 专注审查场景 |

---

## 三、阶段 2：技能设计

### 3.1 技能架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                 CodeReview Assistant 架构                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    接口层 (API)                       │  │
│  │  ├─ review_file(file_path)                          │  │
│  │  ├─ review_code(code_snippet)                       │  │
│  │  ├─ review_pr(pr_data)                              │  │
│  │  └─ configure_rules(rules)                          │  │
│  └────────────────────┬─────────────────────────────────┘  │
│                       │                                     │
│  ┌────────────────────┴─────────────────────────────────┐  │
│  │                    核心引擎                           │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │  │  代码解析器  │  │  规则引擎   │  │  AI 分析器  │  │  │
│  │  │  (Parser)   │→ │  (Rules)   │→ │   (AI)     │  │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  │  │
│  └────────────────────┬─────────────────────────────────┘  │
│                       │                                     │
│  ┌────────────────────┴─────────────────────────────────┐  │
│  │                    知识库层                           │  │
│  │  ├─ 安全规则库     ├─ 性能规则库                       │  │
│  │  ├─ 规范规则库     ├─ 最佳实践库                       │  │
│  │  └─ 语言特性库     └─ 项目特定规则                     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    工具集成层                         │  │
│  │  ├─ AST 解析工具   ├─ 静态分析工具                     │  │
│  │  ├─ Git 工具       ├─ 报告生成工具                     │  │
│  │  └─ CI/CD 插件     └─ IDE 插件                        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 知识库设计

**知识库结构：**

```
knowledge-base/
├── security/                    # 安全规则
│   ├── sql-injection.md
│   ├── xss-prevention.md
│   ├── auth-vulnerabilities.md
│   └── data-exposure.md
├── performance/                 # 性能规则
│   ├── n-plus-one.md
│   ├── memory-leaks.md
│   ├── big-o-analysis.md
│   └── caching-strategies.md
├── quality/                     # 代码质量
│   ├── naming-conventions.md
│   ├── code-complexity.md
│   ├── documentation.md
│   └── testing-best-practices.md
├── languages/                   # 语言特性
│   ├── javascript/
│   ├── typescript/
│   └── python/
└── examples/                    # 示例库
    ├── good-examples/
    └── bad-examples/
```

**安全规则示例（sql-injection.md）：**

```markdown
# SQL 注入防护规则

## 风险等级：🔴 严重

## 描述
SQL 注入是最常见的安全漏洞之一，攻击者可通过构造恶意输入执行任意 SQL 语句。

## 检测模式

### 危险模式（需标记）
1. 字符串拼接 SQL
   ```javascript
   // ❌ 危险
   const query = `SELECT * FROM users WHERE id = ${userId}`;
   ```

2. 未参数化的动态查询
   ```javascript
   // ❌ 危险
   db.query("SELECT * FROM users WHERE name = '" + name + "'");
   ```

### 安全模式（符合规范）
1. 使用参数化查询
   ```javascript
   // ✅ 安全
   const query = 'SELECT * FROM users WHERE id = ?';
   db.query(query, [userId]);
   ```

2. 使用 ORM
   ```javascript
   // ✅ 安全
   const user = await User.findByPk(userId);
   ```

## 修复建议
1. 始终使用参数化查询或预编译语句
2. 使用 ORM 框架（Sequelize、TypeORM、Prisma）
3. 输入验证和转义
4. 最小权限原则（数据库账号权限）

## 参考链接
- [OWASP SQL Injection](https://owasp.org/www-community/attacks/SQL_Injection)
- [CWE-89](https://cwe.mitre.org/data/definitions/89.html)
```

### 3.3 工作流设计

```yaml
workflow:
  name: code_review_process
  
  steps:
    step_1_parse:
      name: 代码解析
      action: parse_code
      description: 解析代码为 AST，提取关键信息
      outputs:
        - ast
        - imports
        - functions
        - classes
    
    step_2_static_analysis:
      name: 静态分析
      action: run_static_analysis
      description: 运行静态规则检查
      inputs:
        - ast
      outputs:
        - static_issues
      tools:
        - eslint
        - ast_parser
    
    step_3_ai_analysis:
      name: AI 深度分析
      action: ai_deep_analysis
      description: AI 分析业务逻辑和潜在问题
      inputs:
        - code_snippet
        - context
        - static_issues
      outputs:
        - ai_issues
        - suggestions
      knowledge:
        - security_rules
        - performance_rules
        - quality_rules
    
    step_4_prioritize:
      name: 问题分级
      action: prioritize_issues
      description: 对问题进行严重程度和优先级排序
      inputs:
        - static_issues
        - ai_issues
      outputs:
        - prioritized_issues
    
    step_5_generate_report:
      name: 生成报告
      action: generate_report
      description: 生成结构化审查报告
      inputs:
        - prioritized_issues
        - suggestions
      outputs:
        - review_report.md
        - review_report.json
```

### 3.4 接口定义

```typescript
// 技能接口定义
interface CodeReviewSkill {
  // 审查单个文件
  reviewFile(filePath: string, options?: ReviewOptions): Promise<ReviewResult>;
  
  // 审查代码片段
  reviewCode(code: string, language: string, options?: ReviewOptions): Promise<ReviewResult>;
  
  // 审查 PR
  reviewPR(prData: PRData, options?: ReviewOptions): Promise<ReviewResult>;
  
  // 配置规则
  configureRules(rules: RuleConfig[]): void;
  
  // 获取支持的规则
  getSupportedRules(): Rule[];
}

// 审查选项
interface ReviewOptions {
  language?: string;           // 编程语言
  severity?: SeverityLevel[];  // 检查的级别
  categories?: Category[];     // 检查的类别
  ignorePatterns?: string[];   // 忽略的文件模式
  maxIssues?: number;          // 最大返回问题数
}

// 审查结果
interface ReviewResult {
  summary: {
    totalFiles: number;
    totalIssues: number;
    severity: {
      critical: number;
      high: number;
      medium: number;
      low: number;
    };
    categories: Record<Category, number>;
  };
  issues: Issue[];
  suggestions: Suggestion[];
  report: string;  // Markdown 格式报告
}

// 问题定义
interface Issue {
  id: string;
  severity: SeverityLevel;
  category: Category;
  title: string;
  description: string;
  location: {
    file: string;
    line: number;
    column: number;
  };
  codeSnippet: string;
  fix?: {
    description: string;
    code: string;
  };
  references: string[];
}
```

---

## 四、阶段 3：开发实现

### 4.1 技能配置文件

```yaml
# skill.yaml
skill:
  name: code-review-assistant
  version: 1.0.0
  description: 智能代码审查助手，支持安全、性能、规范等多维度检查
  
  author: AI Commander Team
  license: MIT
  
  # 元数据
  metadata:
    supported_languages:
      - javascript
      - typescript
      - python
    min_code_size: 10        # 最少代码行数
    max_code_size: 10000     # 最大代码行数
    avg_processing_time: 30s # 平均处理时间
  
  # 触发条件
  triggers:
    commands:
      - /review
      - /check
    file_patterns:
      - "*.js"
      - "*.ts"
      - "*.jsx"
      - "*.tsx"
      - "*.py"
    keywords:
      - "审查"
      - "检查代码"
      - "code review"
  
  # 知识库配置
  knowledge_base:
    path: ./knowledge-base
    auto_reload: true
    categories:
      - security
      - performance
      - quality
      - language_specific
  
  # 工具配置
  tools:
    - name: ast_parser
      description: 代码 AST 解析
      config:
        parser_plugins:
          - typescript
          - jsx
    
    - name: static_analyzer
      description: 静态代码分析
      config:
        rules:
          - no-unused-vars
          - no-console
          - prefer-const
    
    - name: git_integration
      description: Git 集成
      config:
        enabled: true
        diff_context_lines: 3
    
    - name: report_generator
      description: 报告生成
      config:
        formats:
          - markdown
          - json
          - html
  
  # 工作流配置
  workflow:
    default: standard_review
    workflows:
      standard_review:
        steps:
          - parse
          - static_analysis
          - ai_analysis
          - prioritize
          - generate_report
      
      quick_review:
        steps:
          - parse
          - static_analysis
          - generate_report
        description: 快速审查，仅静态分析
      
      deep_review:
        steps:
          - parse
          - static_analysis
          - ai_analysis
          - security_audit
          - performance_audit
          - prioritize
          - generate_report
        description: 深度审查，包含安全和性能审计
  
  # 提示词模板
  prompt_templates:
    analysis: |
      你是资深代码审查专家，请对以下代码进行深度分析。
      
      ## 审查维度
      1. **安全性**：SQL 注入、XSS、敏感信息泄露等
      2. **性能**：时间/空间复杂度、N+1 查询、内存泄漏等
      3. **代码质量**：命名规范、复杂度、重复代码等
      4. **最佳实践**：设计模式、错误处理、边界情况等
      
      ## 代码
      ```{language}
      {code}
      ```
      
      ## 上下文
      {context}
      
      ## 已知问题（来自静态分析）
      {static_issues}
      
      ## 输出格式
      对每个发现的问题，请按以下格式输出：
      
      ### [{severity}] {category} - {title}
      - **位置**：{file}:{line}
      - **问题描述**：{description}
      - **潜在影响**：{impact}
      - **修复建议**：{suggestion}
      - **修复代码**：
        ```{language}
        {fixed_code}
        ```
    
    report: |
      根据以下审查结果生成结构化报告：
      
      ## 审查概览
      - 审查文件数：{total_files}
      - 发现问题数：{total_issues}
      - 严重问题：{critical_count}
      - 建议优化：{suggestions_count}
      
      ## 详细问题列表
      {issues_list}
      
      ## 修复优先级
      {priority_suggestions}
```

### 4.2 核心代码实现

**主入口文件：**

```typescript
// src/index.ts
import { SkillEngine } from './core/SkillEngine';
import { CodeParser } from './tools/CodeParser';
import { StaticAnalyzer } from './tools/StaticAnalyzer';
import { AIAnalyzer } from './tools/AIAnalyzer';
import { ReportGenerator } from './tools/ReportGenerator';
import { KnowledgeBase } from './knowledge/KnowledgeBase';

export class CodeReviewAssistant {
  private engine: SkillEngine;
  private knowledgeBase: KnowledgeBase;
  
  constructor(config: SkillConfig) {
    // 初始化知识库
    this.knowledgeBase = new KnowledgeBase(config.knowledge_base);
    
    // 初始化工具
    const tools = {
      parser: new CodeParser(config.tools.ast_parser),
      staticAnalyzer: new StaticAnalyzer(config.tools.static_analyzer),
      aiAnalyzer: new AIAnalyzer({
        knowledgeBase: this.knowledgeBase,
        promptTemplate: config.prompt_templates.analysis
      }),
      reportGenerator: new ReportGenerator(config.tools.report_generator)
    };
    
    // 初始化引擎
    this.engine = new SkillEngine({
      workflow: config.workflow,
      tools: tools
    });
  }
  
  async reviewFile(filePath: string, options?: ReviewOptions): Promise<ReviewResult> {
    const context = {
      type: 'file',
      path: filePath,
      options
    };
    
    return this.engine.execute('standard_review', context);
  }
  
  async reviewCode(code: string, language: string, options?: ReviewOptions): Promise<ReviewResult> {
    const context = {
      type: 'code',
      code,
      language,
      options
    };
    
    return this.engine.execute('standard_review', context);
  }
  
  async reviewPR(prData: PRData, options?: ReviewOptions): Promise<ReviewResult> {
    const context = {
      type: 'pr',
      prData,
      options
    };
    
    return this.engine.execute('deep_review', context);
  }
}
```

**AI 分析器实现：**

```typescript
// src/tools/AIAnalyzer.ts
import { OpenAI } from 'openai';

export class AIAnalyzer {
  private openai: OpenAI;
  private knowledgeBase: KnowledgeBase;
  private promptTemplate: string;
  
  constructor(config: AIAnalyzerConfig) {
    this.openai = new OpenAI({ apiKey: config.apiKey });
    this.knowledgeBase = config.knowledgeBase;
    this.promptTemplate = config.promptTemplate;
  }
  
  async analyze(
    code: string, 
    language: string, 
    context: AnalysisContext,
    staticIssues: StaticIssue[]
  ): Promise<AIssue[]> {
    // 从知识库获取相关规则
    const relevantRules = await this.knowledgeBase.getRelevantRules(code, language);
    
    // 构建提示词
    const prompt = this.buildPrompt({
      code,
      language,
      context,
      staticIssues,
      rules: relevantRules
    });
    
    // 调用 AI 分析
    const response = await this.openai.chat.completions.create({
      model: 'gpt-4',
      messages: [
        { role: 'system', content: '你是专业的代码审查专家。' },
        { role: 'user', content: prompt }
      ],
      temperature: 0.3,
      max_tokens: 4000
    });
    
    // 解析 AI 输出
    const analysis = response.choices[0].message.content;
    return this.parseAnalysis(analysis);
  }
  
  private buildPrompt(params: PromptParams): string {
    return this.promptTemplate
      .replace('{language}', params.language)
      .replace('{code}', params.code)
      .replace('{context}', JSON.stringify(params.context))
      .replace('{static_issues}', JSON.stringify(params.staticIssues))
      .replace('{rules}', params.rules.join('\n'));
  }
  
  private parseAnalysis(analysis: string): AIssue[] {
    // 解析 AI 输出，提取结构化问题
    const issues: AIssue[] = [];
    
    // 使用正则表达式提取问题块
    const issueBlocks = analysis.match(/### \[.+?\][\s\S]+?(?=### |$)/g) || [];
    
    for (const block of issueBlocks) {
      issues.push(this.parseIssueBlock(block));
    }
    
    return issues;
  }
  
  private parseIssueBlock(block: string): AIssue {
    // 解析单个问题块
    const severityMatch = block.match(/\[(\w+)\]/);
    const titleMatch = block.match(/-\s*([^\n]+)/);
    
    return {
      severity: severityMatch?.[1] as SeverityLevel || 'medium',
      title: titleMatch?.[1] || 'Unknown Issue',
      // ... 其他字段解析
    };
  }
}
```

### 4.3 工具集成

**Git Hook 集成：**

```javascript
// .git/hooks/pre-commit
#!/bin/sh
# 代码提交前自动审查

# 获取暂存区的文件
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(js|ts|jsx|tsx|py)$')

if [ -z "$STAGED_FILES" ]; then
  exit 0
fi

echo "🔍 正在审查代码..."

# 调用技能进行审查
npx code-review-assistant review --files "$STAGED_FILES" --format simple

if [ $? -ne 0 ]; then
  echo "❌ 代码审查未通过，请修复问题后重新提交"
  exit 1
fi

echo "✅ 代码审查通过"
exit 0
```

**CI/CD 集成（GitHub Actions）：**

```yaml
# .github/workflows/code-review.yml
name: Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install Dependencies
        run: npm ci
      
      - name: Run Code Review
        id: review
        run: |
          npx code-review-assistant review-pr \
            --pr ${{ github.event.pull_request.number }} \
            --output report.md
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      
      - name: Post Review Comment
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('report.md', 'utf8');
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: report
            });
```

---

## 五、阶段 4：测试与优化

### 5.1 测试策略

```
测试金字塔
                    ┌─────────┐
                    │  E2E   │  ← 完整工作流测试
                    │  10%   │
                   ┌┴─────────┴┐
                   │ Integration│ ← 工具集成测试
                   │    30%    │
                  ┌┴───────────┴┐
                  │   Unit      │ ← 核心逻辑单元测试
                  │    60%     │
                  └─────────────┘
```

**单元测试示例：**

```typescript
// tests/AIAnalyzer.test.ts
import { AIAnalyzer } from '../src/tools/AIAnalyzer';
import { KnowledgeBase } from '../src/knowledge/KnowledgeBase';

describe('AIAnalyzer', () => {
  let analyzer: AIAnalyzer;
  let mockKnowledgeBase: jest.Mocked<KnowledgeBase>;
  
  beforeEach(() => {
    mockKnowledgeBase = {
      getRelevantRules: jest.fn()
    } as any;
    
    analyzer = new AIAnalyzer({
      apiKey: 'test-key',
      knowledgeBase: mockKnowledgeBase,
      promptTemplate: 'Analyze: {code}'
    });
  });
  
  test('should detect SQL injection vulnerability', async () => {
    const vulnerableCode = `
      const query = "SELECT * FROM users WHERE id = " + userId;
      db.query(query);
    `;
    
    mockKnowledgeBase.getRelevantRules.mockResolvedValue([
      'Always use parameterized queries'
    ]);
    
    const issues = await analyzer.analyze(
      vulnerableCode,
      'javascript',
      {},
      []
    );
    
    expect(issues).toContainEqual(
      expect.objectContaining({
        category: 'security',
        title: expect.stringContaining('SQL')
      })
    );
  });
  
  test('should handle empty code gracefully', async () => {
    const issues = await analyzer.analyze('', 'javascript', {}, []);
    expect(issues).toHaveLength(0);
  });
});
```

### 5.2 效果评估

**评估指标：**

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| **准确率** | > 90% | 人工审查对比 |
| **召回率** | > 85% | 已知问题检测率 |
| **误报率** | < 10% | 错误报告占比 |
| **平均耗时** | < 30s | 1000 行代码 |
| **用户满意度** | > 4.0/5 | 用户反馈调查 |

**A/B 测试方案：**

```
对照组：纯人工 Code Review
实验组：AI 辅助 Code Review

测量指标：
1. 审查时间
2. 发现问题数
3. 遗漏问题数
4. 开发者满意度
```

### 5.3 持续优化

**反馈循环：**

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  用户使用 │───→│  收集反馈 │───→│  分析问题 │───→│  优化技能 │
└──────────┘    └──────────┘    └──────────┘    └────┬─────┘
                                                     │
                         └───────────────────────────┘
```

**优化方向：**

1. **知识库扩充**
   - 添加更多语言支持
   - 补充框架特定规则
   - 积累项目特定模式

2. **提示词优化**
   - 根据反馈调整提示词
   - 添加 Few-shot 示例
   - 优化输出格式

3. **工具增强**
   - 集成更多静态分析工具
   - 支持更多报告格式
   - 优化性能

---

## 六、本节小结

### 核心流程

技能开发的四个阶段：
1. **需求分析**：明确目标用户、使用场景和功能需求
2. **技能设计**：设计架构、知识库、工作流和接口
3. **开发实现**：编写配置、实现核心逻辑、集成工具
4. **测试优化**：单元测试、集成测试、效果评估和持续优化

### 关键要点

- **知识库是核心**：高质量的知识库决定技能的专业程度
- **工作流要清晰**：明确的步骤和输入输出保证可预期性
- **工具集成要灵活**：支持多种使用场景（CLI、CI/CD、IDE）
- **持续优化是关键**：通过反馈不断提升技能效果

### 技能开发检查清单

- [ ] 明确技能目标和用户
- [ ] 设计完整的知识库结构
- [ ] 定义清晰的工作流
- [ ] 编写详细的提示词模板
- [ ] 实现核心逻辑
- [ ] 集成必要的工具
- [ ] 编写测试用例
- [ ] 评估技能效果
- [ ] 建立反馈机制

### 下一步

在下一节课 [L5-3: 知识库构建与管理](/tutorial/L5-3) 中，我们将：
- 深入了解知识库的构建方法
- 学习如何组织和维护知识
- 掌握知识检索和更新技巧
- 实战：构建一个前端开发知识库

---

→ [5.3 知识库构建与管理](/tutorial/L5-3)
