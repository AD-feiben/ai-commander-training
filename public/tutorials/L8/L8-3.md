# L8-3: 性能优化策略

> 掌握系统化的性能优化方法

## 本节导读

性能优化是一个系统工程，需要从多个维度进行优化。本节课将介绍前端和后端的性能优化策略。

通过本节课，你将学会：
- 前端性能优化技巧
- 后端性能优化方法
- 数据库优化策略
- 缓存设计原则

---

## 一、前端性能优化

### 1.1 加载优化

```javascript
// 1. 代码分割
const Dashboard = lazy(() => import('./Dashboard'));

// 2. 预加载关键资源
<link rel="preload" href="/critical.css" as="style">
<link rel="prefetch" href="/next-page.js">

// 3. 图片优化
<img 
  src="image.webp" 
  loading="lazy" 
  decoding="async"
  width="800"
  height="600"
/>
```

### 1.2 渲染优化

```javascript
// 1. 使用 useMemo 缓存计算
const processedData = useMemo(() => {
  return data.map(processItem);
}, [data]);

// 2. 使用 useCallback 缓存函数
const handleClick = useCallback(() => {
  onItemClick(id);
}, [id, onItemClick]);

// 3. 虚拟列表
import { FixedSizeList } from 'react-window';

<FixedSizeList
  height={500}
  itemCount={10000}
  itemSize={50}
>
  {Row}
</FixedSizeList>

// 4. 防抖节流
const debouncedSearch = useMemo(
  () => debounce(handleSearch, 300),
  [handleSearch]
);
```

### 1.3 网络优化

```javascript
// 1. 使用 Service Worker 缓存
// sw.js
self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request).then((response) => {
      return response || fetch(event.request);
    })
  );
});

// 2. HTTP 缓存策略
// Cache-Control: public, max-age=31536000, immutable
```

---

## 二、后端性能优化

### 2.1 数据库优化

```javascript
// 1. 索引优化
// 创建复合索引
db.collection('users').createIndex({ email: 1, status: 1 });

// 2. 查询优化
// ❌ 避免
const users = await User.find();
const activeUsers = users.filter(u => u.status === 'active');

// ✅ 推荐
const activeUsers = await User.find({ status: 'active' });

// 3. 分页优化
// 使用游标分页
db.collection('posts')
  .find({ _id: { $lt: lastId } })
  .limit(20)
  .sort({ _id: -1 });
```

### 2.2 缓存策略

```javascript
// 1. Redis 缓存
const cacheKey = `user:${userId}`;
let user = await redis.get(cacheKey);

if (!user) {
  user = await db.users.findById(userId);
  await redis.setex(cacheKey, 3600, JSON.stringify(user));
}

// 2. 本地缓存
const localCache = new Map();
const CACHE_TTL = 5 * 60 * 1000; // 5分钟

function getCached(key, fetcher) {
  const cached = localCache.get(key);
  if (cached && Date.now() - cached.time < CACHE_TTL) {
    return cached.data;
  }
  
  const data = fetcher();
  localCache.set(key, { data, time: Date.now() });
  return data;
}
```

### 2.3 异步处理

```javascript
// 1. 消息队列
const queue = new Bull('email-queue');

// 生产者
queue.add('send-email', { to, subject, body });

// 消费者
queue.process('send-email', async (job) => {
  await sendEmail(job.data);
});

// 2. 批处理
const batchProcessor = new BatchProcessor({
  batchSize: 100,
  flushInterval: 1000,
  processor: async (items) => {
    await db.bulkInsert(items);
  }
});
```

---

## 三、数据库优化策略

### 3.1 查询优化

```sql
-- 1. 使用 EXPLAIN 分析查询
EXPLAIN ANALYZE SELECT * FROM users WHERE email = 'test@example.com';

-- 2. 避免 SELECT *
SELECT id, name, email FROM users WHERE status = 'active';

-- 3. 使用 JOIN 代替子查询
-- ❌ 避免
SELECT * FROM orders WHERE user_id IN (SELECT id FROM users WHERE status = 'active');

-- ✅ 推荐
SELECT o.* FROM orders o
JOIN users u ON o.user_id = u.id
WHERE u.status = 'active';
```

### 3.2 索引策略

```sql
-- 1. 单列索引
CREATE INDEX idx_users_email ON users(email);

-- 2. 复合索引
CREATE INDEX idx_orders_user_date ON orders(user_id, created_at);

-- 3. 部分索引
CREATE INDEX idx_active_users ON users(email) WHERE status = 'active';

-- 4. 覆盖索引
CREATE INDEX idx_users_covering ON users(email, name, phone);
-- 查询 SELECT email, name, phone FROM users WHERE email = '...' 可以直接从索引获取
```

---

## 四、本节小结

### 核心要点

1. **前端优化**：代码分割、懒加载、虚拟列表、防抖节流

2. **后端优化**：数据库索引、查询优化、缓存策略、异步处理

3. **数据库优化**：EXPLAIN 分析、避免 SELECT *、合理使用索引

4. **缓存设计**：多级缓存、TTL 策略、缓存穿透防护

### 下一步

在下一节课 [L8-4: 性能监控与告警](/tutorial/L8-4) 中，我们将：
- 学习性能监控体系搭建
- 掌握监控指标选择
- 了解告警策略设计
- 实战：构建性能监控 dashboard

---

→ [8.4 性能监控与告警](/tutorial/L8-4)
