# L8-4: 性能监控与告警

> 建立完善的性能监控体系

## 本节导读

性能优化不是一次性的工作，需要持续监控和改进。本节课将介绍如何建立性能监控体系和告警机制。

通过本节课，你将学会：
- 性能监控指标选择
- 监控工具使用
- 告警策略设计
- 实战：构建监控 dashboard

---

## 一、监控指标体系

### 1.1 RUM（真实用户监控）

```javascript
// 使用 web-vitals 库
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals';

function sendToAnalytics(metric) {
  const body = JSON.stringify(metric);
  
  // 发送到分析服务
  fetch('/analytics', {
    body,
    method: 'POST',
    keepalive: true,
  });
}

getCLS(sendToAnalytics);
getFID(sendToAnalytics);
getFCP(sendToAnalytics);
getLCP(sendToAnalytics);
getTTFB(sendToAnalytics);
```

### 1.2 自定义性能标记

```javascript
// 标记关键时间点
performance.mark('app-start');
performance.mark('data-loaded');

// 测量时间间隔
performance.measure('data-loading', 'app-start', 'data-loaded');

// 获取测量结果
const measures = performance.getEntriesByType('measure');
```

---

## 二、监控工具

### 2.1 前端监控

```javascript
// 错误监控
window.addEventListener('error', (event) => {
  reportError({
    type: 'javascript',
    message: event.message,
    filename: event.filename,
    lineno: event.lineno,
    colno: event.colno,
    stack: event.error?.stack
  });
});

// 性能监控
const observer = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    if (entry.entryType === 'largest-contentful-paint') {
      console.log('LCP:', entry.startTime);
    }
  }
});

observer.observe({ entryTypes: ['largest-contentful-paint'] });
```

### 2.2 后端监控

```javascript
// Express 中间件
app.use((req, res, next) => {
  const start = Date.now();
  
  res.on('finish', () => {
    const duration = Date.now() - start;
    
    metrics.timing('http.request', duration, {
      method: req.method,
      route: req.route?.path,
      status: res.statusCode
    });
  });
  
  next();
});

// 数据库查询监控
const originalQuery = db.query.bind(db);
db.query = async (...args) => {
  const start = Date.now();
  const result = await originalQuery(...args);
  const duration = Date.now() - start;
  
  metrics.timing('db.query', duration);
  
  return result;
};
```

---

## 三、告警策略

### 3.1 告警规则

```yaml
# alerting-rules.yaml
alerts:
  - name: high_error_rate
    condition: error_rate > 5%
    duration: 5m
    severity: critical
    
  - name: slow_response
    condition: p95_response_time > 1000ms
    duration: 10m
    severity: warning
    
  - name: low_apdex
    condition: apdex_score < 0.7
    duration: 15m
    severity: warning
    
  - name: high_cpu
    condition: cpu_usage > 80%
    duration: 10m
    severity: warning
```

### 3.2 告警通知

```javascript
// 告警处理器
class AlertManager {
  async handleAlert(alert) {
    // 根据严重程度选择通知渠道
    if (alert.severity === 'critical') {
      await this.sendPagerDuty(alert);
      await this.sendSlack(alert);
      await this.sendEmail(alert);
    } else if (alert.severity === 'warning') {
      await this.sendSlack(alert);
    }
    
    // 记录告警
    await this.logAlert(alert);
  }
  
  // 告警抑制
  shouldSuppress(alert) {
    // 相同告警在 30 分钟内不重复发送
    const key = `${alert.name}:${alert.labels}`;
    const lastAlert = this.alertCache.get(key);
    
    if (lastAlert && Date.now() - lastAlert.time < 30 * 60 * 1000) {
      return true;
    }
    
    this.alertCache.set(key, { time: Date.now() });
    return false;
  }
}
```

---

## 四、实战：监控 Dashboard

### 4.1 Dashboard 设计

```
┌─────────────────────────────────────────────────────────────┐
│                    性能监控 Dashboard                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │   FCP    │  │   LCP    │  │   FID    │  │   CLS    │    │
│  │  1.2s    │  │  2.1s    │  │  45ms    │  │  0.02    │    │
│  │   ✅     │  │   ✅     │  │   ✅     │  │   ✅     │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                 响应时间趋势                          │  │
│  │                                                      │  │
│  │    ╱╲        ╱╲                                     │  │
│  │   ╱  ╲      ╱  ╲      ╱╲                           │  │
│  │  ╱    ╲____╱    ╲____╱  ╲_____________________     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────┐  ┌──────────────────────────┐   │
│  │    错误率趋势         │  │     吞吐量趋势            │   │
│  │                      │  │                         │   │
│  └──────────────────────┘  └──────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 实现代码

```typescript
// Dashboard 组件
export function PerformanceDashboard() {
  const { data, loading } = usePerformanceData();
  
  if (loading) return <Loading />;
  
  return (
    <div className="dashboard">
      <MetricCards metrics={data.coreWebVitals} />
      <ResponseTimeChart data={data.responseTime} />
      <div className="grid grid-cols-2">
        <ErrorRateChart data={data.errorRate} />
        <ThroughputChart data={data.throughput} />
      </div>
      <AlertList alerts={data.alerts} />
    </div>
  );
}

// 指标卡片
function MetricCards({ metrics }) {
  return (
    <div className="metric-cards">
      {metrics.map(metric => (
        <Card key={metric.name}>
          <h3>{metric.name}</h3>
          <div className={metric.status}>
            {metric.value}
          </div>
          <div className="target">
            目标: {metric.target}
          </div>
        </Card>
      ))}
    </div>
  );
}
```

---

## 五、本节小结

### 核心要点

1. **监控指标**：RUM、自定义标记、核心 Web 指标

2. **监控工具**：web-vitals、PerformanceObserver、中间件

3. **告警策略**：分级告警、告警抑制、多渠道通知

4. **Dashboard**：可视化展示、实时更新、历史趋势

### 下一步

在下一节课 [L8-5: 性能优化案例](/tutorial/L8-5) 中，我们将：
- 分析真实性能优化案例
- 学习优化决策过程
- 了解优化效果评估
- 实战：端到端性能优化项目

---

→ [8.5 性能优化案例](/tutorial/L8-5)
